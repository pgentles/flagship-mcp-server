{
  "name": "flagship-mcp-server",
  "version": "1.0.0",
  "type": "commonjs",
  "main": "dist/server.js",
  "scripts": {
    "build": "esbuild server.ts --bundle --platform=node --target=node20 --outfile=dist/server.js --external:axios --external:zod --format=cjs",
    "start": "node dist/server.js",
    "dev": "node dist/server.js"
  },
  "dependencies": {
    "@modelcontextprotocol/sdk": "^1.9.0",
    "axios": "^1.13.2",
    "express": "^5.2.1",
    "zod": "^3.23.0"
  },
  "devDependencies": {
    "@types/express": "^5.0.6",
    "esbuild": "^0.21.0"
  }
}
